# 开发进度

## 2026-04-15

### 已完成
- **需求文档**（`网管系统模拟工具-需求分析.md` v3.2）
  - 按 PRD 规范重构：执行摘要、用户画像、用户故事 + 验收标准、非目标、风险
  - 移除全部代码 / 表结构 / 架构图
  - 落定：接口启用开关、接口-拓扑绑定下拉框、多拓扑并行编辑、自动保存 60s、历史版本 3 个、导出不加密、不内置接口模板
- **系统架构设计**（`系统架构设计.md` v1.0）
  - 前后端独立部署；后端单进程 FastAPI，路径前缀区分 `/admin/api/*` 与 `/mock/*`
  - SQLite 唯一数据源（无内存副本）
  - 前端 Vue 3 SPA + AntV X6 + Ant Design Vue + pnpm
  - 后端 Python 3.11+ + FastAPI + Pydantic v2 + SQLite
  - WebSocket 承载保存状态 / 日志流 / 请求推送
  - 模块划分、SQLite 表清单、请求处理管道、动态路由同步、目录结构与 `.env` 配置

- **数据库表设计**（`数据库表设计.md` v1.0）
  - 13 张表 DDL：拓扑 / 历史 / 设备类型 / 设备 / 属性 / 端口 / 链路 / 画布 / 接口 / Token / 日志 / 设置
  - 采用扁平关系表（非 JSON blob），固定字段存列、自定义属性 K-V 存副表
  - 接口配置仅 `method/path/enabled/topology_id/sql_text` 上列，嵌套结构进 `config` JSON
  - 明确外键级联与业务层删除约束（拓扑被接口引用时阻止删除）
  - 索引策略 + SQL 模式视图隔离方案（拓扑作用域内查询）
  - 请求日志滚动 10000 条；历史版本保留 3 个

- **数据库表设计 v2.0**（重构）
  - 放弃 devices / ports / links 专表，采用**统一节点 + 统一边**模型，覆盖物理 / 云 / 应用三层
  - 取消 `parent_id`，父子关系用 `contain` 语义的边表达，通过 `edge_types.exclusive_target` 约束"只能有一个父"
  - `node_types.render_mode` 决定可视化方式：container（嵌套）/ anchor（父节点锚点）/ flat（层级线）/ none
  - 新增 15 张表：含 `node_types/node_type_fields/edge_types/edge_type_fields/nodes/node_attrs/edges/edge_attrs` 等
  - SQL 模式改为**动态 CTE 视图生成**：按绑定拓扑中出现的类型，每类型一个同名视图（字段=固定列+属性打平）；始终提供 `nodes/edges/children` 通用视图
  - 预置 11 个节点类型（交换机 / 路由器 / ECS / 宿主机 / Pod / 容器……）与 5 个边类型（contains / physical_link / deployed_on / depends_on / connects）

- **API 接口设计**（`API接口设计.md` v1.0）
  - 三通道划分：`/admin/api/**`（管理）、`/admin/ws`（实时）、`/mock/**`（模拟）
  - 管理 API 覆盖 11 个模块：拓扑 / 节点类型 / 边类型 / 节点 / 边 / 接口配置 / SQL 辅助 / Token / 日志 / 系统设置 / 仪表盘
  - 统一响应结构、错误码分段、前端 camelCase + 服务层 snake_case 映射
  - 节点批量创建支持自动建 parent 边；画布位置单独 PATCH 接口
  - 接口配置含测试执行（不计日志） + 启用开关 / 拓扑切换 PATCH
  - SQL 辅助：动态 CTE 视图清单、预览、直接执行
  - WebSocket 订阅模型 + 7 类事件 + ping/pong 心跳

## 2026-04-16

### 已完成
- **开发方案**（`开发方案.md` v1.0）
  - 7 章：开发规范 / 关键技术实现 / 脚手架 / WBS / 工具链 / 风险 / 变更
  - WBS 四个里程碑（M1-M4）共 34 个任务，每个任务内嵌验收标准
  - 关键技术点 10 项：动态路由、CTE 生成、SQL 白名单、参数绑定、模板渲染、Token、自动保存、WS、日志滚动、画布性能
  - 风险清单聚焦 MVP：SQLite 并发 / CTE 拼接 / 大图性能 / OpenAPI 冲突 / Windows 编码

### 下一步
- 项目脚手架初始化（M1-01 / M1-02）
- 前端页面与交互细节设计（可与 M1 并行）

## 2026-04-17

### 已完成
- **调整 Claude Code skills 配置**：移除与技术栈不符的 React 相关 skill（`typescript-react-reviewer`、`vercel-react-best-practices`）；安装并精简如下 skills（最终链接到 Claude Code 的 15 个）：
  - 前端设计：`frontend-design`
  - 前端开发：`vue`、`vue-best-practices`、`vueuse-functions`、`vite`、`pinia`
  - 前端测试：`vue-testing-best-practices`、`playwright-best-practices`
  - 后端架构：`fastapi-templates`、`async-python-patterns`、`database-design-expert`
  - 后端开发：`python-performance-optimization`、`sqlite-database-expert`、`websocket-engineer`
  - 后端测试：`pytest-coverage`
  - 注：另已移除 sqlite 包带入的 ~50 个无关 skills（`talos-os-expert`、`graphql-expert`、`tauri` 等）；项目级原有 `fastapi-python`、`pydantic`、`python-testing-patterns`、`code-review-quality`、`ant-design`、`webapp-testing` 保留

- **M1-01 后端脚手架（S1-S6）完成**
  - 目录：`backend/app/{admin,mock,core,models,db}` 骨架 + `requirements.txt`（fastapi / uvicorn / pydantic>=2 / python-dotenv）
  - `app/core/config.py`：从 `.env` 读取 APP_HOST / APP_PORT / DB_PATH / LOG_LEVEL / REQUEST_LOG_MAX / AUTOSAVE_INTERVAL
  - `app/db/connection.py`：sqlite3 连接（`PRAGMA foreign_keys=ON` + WAL 模式 + `synchronous=NORMAL`），提供 `connect()` / `transaction()` 两种上下文管理器
  - `app/db/migrations.py`：按《数据库表设计》v2.3 DDL 建 14 张表 + 索引（全部 `IF NOT EXISTS`，重复启动幂等）
  - `app/db/seed.py`：预置 11 节点类型（含示例字段） + 5 边类型 + 3 项系统设置（`autosave_interval`、`request_log_max`、`mock_path_prefix`），`INSERT OR IGNORE` 幂等
  - `app/main.py`：FastAPI `lifespan` 钩子内调用 `init_db()` 自动建库；挂载 `/admin/api/health`（返回 `{code:0,...}`）
  - `backend/.env.example`、仓库根 `start.sh` / `start.bat`
  - 验收：`python -m uvicorn app.main:app` 启动后 `curl /admin/api/health` 返回 `{"code":0,"data":{"status":"ok"},"message":"ok"}`，HTTP 200；DB 包含 14 张业务表 + 16 条预置数据

- **M1-02 前端脚手架（F1-F6）完成**
  - `frontend/`：Vite 8 + Vue 3.5 + TypeScript SPA，`pnpm dev` 启动于 `localhost:5173`
  - 依赖：`ant-design-vue@4.2` / `@antv/x6@3.1` / `pinia@3` / `axios@1` / `vue-router@5` / `@ant-design/icons-vue@7`
  - `vite.config.ts`：proxy 代理 `/admin/api` + `/admin/ws`（ws）+ `/mock` → `http://localhost:8080`（可通过 `VITE_BACKEND` 环境变量覆盖）
  - `src/api/http.ts`：axios 封装，响应拦截自动解析 `code` 错误 + `camelizeKeys` snake_case→camelCase 转换；`apiGet/Post/Put/Patch/Delete` 五个快捷方法
  - `src/api/case.ts`：`camelizeKeys` 深度递归转换
  - `src/ws/client.ts`：`WsClient` 单例，指数退避重连（最大 30s），`ping/pong` 心跳（30s），按 topic 订阅/退订，事件委托
  - `src/router/index.ts` + `src/layouts/AppLayout.vue`：Ant Design Layout，侧栏暗色可折叠，含 7 个菜单项（仪表盘/拓扑/类型管理/接口/Token/日志/设置），顶栏页面标题，Vue Router 5 history 模式
  - `src/views/`：7 个空白占位页（DashboardView 含健康探测按钮），主 `App.vue` 挂载 Pinia + Router + Antd
  - 验收（Playwright）：7 菜单全部可见可点击；`GET /admin/api/health` 经 Vite proxy 返回 `{code:0,...}`；无控制台错误；`pnpm build` 成功（ant-design-vue chunk 1.4 MB 正常）

- **M1-03 拓扑 CRUD API 完成**（并按 API 接口设计规范完善异常处理）
  - `backend/app/admin/schemas/topology.py`：Pydantic v2 模型（TopologyCreate/Update/Detail/ListItem/Stats/Graph 等）
  - `backend/app/admin/topology.py`：7 个路由实现（Python 3.9 兼容，用 `Optional` 代替 `str | None`）
    - `GET /admin/api/topologies` — 分页列表，支持 name 模糊过滤、sort 排序
    - `GET /admin/api/topologies/{id}` — 详情 + node_count/edge_count 统计
    - `POST /admin/api/topologies` — 新建，`topo_{uuid12}` 格式 ID；**重复名称返回 409 + code:40003**
    - `PUT /admin/api/topologies/{id}` — 更新 name/description；**无更新字段返回 400 + code:40001**
    - `DELETE /admin/api/topologies` — **批量删除全部拓扑**；返回 deletedCount；有关联 api_configs 时返回 409 + code:40103
    - `DELETE /admin/api/topologies/{id}` — 删除；**不存在返回 404 + code:40101；被引用返回 409 + code:40103**
    - `GET /admin/api/topologies/{id}/graph` — 完整图数据（节点+边+画布坐标）；**不存在返回 404 + code:40101**
  - 统一错误响应格式：`{"detail": {"code": xxx, "message": "...", "details": {...}}}`
  - 已在 uvicorn 下验证全部异常场景

- **M1-04 拓扑列表页 完成**
  - `src/api/topology.ts`：TypeScript 类型定义（TopologyStats/ListItem/Detail/Create/Update/PageResult）
  - `src/composables/useTopologies.ts`：`useTopologies` 组合函数封装 CRUD + 分页/搜索/排序状态
  - `src/components/topology/TopologyTable.vue`：Ant Design Table + 分页 + 排序 + 名称搜索 + 新建/编辑/删除操作列
  - `src/components/topology/TopologyModal.vue`：新建/编辑拓扑 Modal 表单（Pydantic 校验）
  - `src/views/TopologiesView.vue`：组合 surface，协调 modal 与 table
  - 验收：`pnpm build` 通过（ant-design-vue chunk 1.4 MB 正常）

- **M1-05 拓扑画布页 完成**
  - `src/api/topology.ts`：新增 TopologyCanvasNode/TopologyNode/TopologyEdge/TopologyGraph 类型
  - `src/composables/useCanvas.ts`：`useCanvas` 组合函数封装 graph 状态、positions 保存逻辑
  - `src/components/canvas/TopologyCanvas.vue`：AntV X6 Graph 初始化、节点/边渲染、History 插件（撤销/重做）、位置变更事件
  - `src/components/canvas/CanvasToolbar.vue`：工具栏含保存/撤销/重做/放大/缩小/适应画布，状态角标（未保存/保存中/已保存）
  - `src/views/CanvasView.vue`：画布页面，路由 `topologies/:id/canvas`
  - `backend/app/admin/topology.py`：新增 `PATCH /topologies/{id}/canvas` 保存节点坐标
  - `frontend/src/router/index.ts`：新增 `/topologies/:id/canvas` 路由
  - **拓扑列表页入口**：TopologyTable 操作列新增"进入画布"按钮，TopologiesView 通过 `router.push` 跳转画布页面
  - 验收：`pnpm build` 通过；Playwright E2E 测试全部通过（列表/工具栏/保存按钮/画布页面跳转/无控制台错误）

- **M2-01 node_types / edge_types CRUD API 完成**
  - `backend/app/admin/schemas/node_type.py`：Pydantic v2 模型（NodeType/EdgeType + Field CRUD）
  - `backend/app/admin/node_type.py`：14个路由实现（Python 3.9 兼容）
    - node_types：GET列表/GET详情/POST新建/PUT更新/DELETE删除
    - node_type_fields：POST新增/PUT更新/DELETE删除
    - edge_types：GET列表/GET详情/POST新建/PUT更新/DELETE删除
    - edge_type_fields：POST新增/PUT更新/DELETE删除
  - 统一错误码：40201(不存在)/40202(code重复)/40204(field重复)/40301~40305(edge同系)
  - 删除被使用类型返回 409 + code:40201/40301
  - 已在 uvicorn 下验证全部异常场景

- **M2-02 节点 CRUD API 完成**（含 `PATCH /nodes/{id}/position`）
  - `backend/app/admin/schemas/node.py`：Pydantic v2 模型（NodeCreate/Update/Detail/PositionUpdate/AttrSet）
  - `backend/app/admin/node.py`：9个路由实现
    - `GET /nodes` — 列表（支持 topology_id/node_type_id/status 过滤+分页）
    - `GET /nodes/{id}` — 详情（含 attrs 和 canvas 坐标）
    - `POST /nodes` — 新建（校验拓扑/类型存在、dn 唯一性）
    - `PUT /nodes/{id}` — 更新（dn 唯一性校验）
    - `DELETE /nodes/{id}` — 删除（CASCADE 删 attrs/canvas）
    - `PATCH /nodes/{id}/position` — 更新单节点坐标
    - `PUT /nodes/{id}/attrs` — 批量设置节点属性
  - 错误码：40401(不存在)/40402(拓扑不存在)/40403(类型不存在)/40404(dn重复)/40405(无更新字段)
  - 已在 uvicorn 下验证全部异常场景

- **M2-03 边 CRUD API 完成**
  - `backend/app/admin/schemas/edge.py`：Pydantic v2 模型（EdgeCreate/Update/Detail/Item/AttrSet）
  - `backend/app/admin/edge.py`：5个路由实现
    - `GET /topologies/{topologyId}/edges` — 列表（支持 edge_type_id/source_id/target_id 过滤+分页）
    - `GET /edges/{id}` — 详情（含 attrs）
    - `POST /topologies/{topologyId}/edges` — 新建（校验拓扑/边类型/源节点/目标节点/类型白名单/exclusive_target 约束）
    - `PUT /edges/{id}` — 更新（仅 status）
    - `DELETE /edges/{id}` — 删除（CASCADE 删 attrs）
  - 错误码：40101(拓扑不存在)/40001(边类型不存在)/40102(源节点不存在)/40106(目标节点不存在/边不存在)/40107(跨拓扑)/40108(exclusive_target冲突)/40001(无更新字段)
  - exclusive_target 约束：对于 `et_contains` 类型，同一 target 只能有一条入边
  - 已在 uvicorn 下验证全部异常场景
  - Postman 测试脚本：`test_scripts/postman_edges.json`（22 个测试用例）

- **M2-07 类型管理页（前端）完成**
  - `src/api/types.ts`：TypeScript 类型定义（NodeType/EdgeType + Field CRUD） + API 方法封装
  - `src/composables/useTypes.ts`：`useNodeTypes` / `useEdgeTypes` 组合函数封装 CRUD + 状态管理
  - `src/components/types/NodeTypeTable.vue`：节点类型表格 + 可展开字段列表 + 新建/编辑/删除操作
  - `src/components/types/NodeTypeModal.vue`：新建/编辑节点类型 Modal 表单
  - `src/components/types/NodeTypeFieldEditor.vue`：可视化字段管理（添加/编辑/删除，支持 text/number/select/boolean 类型）
  - `src/components/types/EdgeTypeTable.vue`：边类型表格 + 可展开字段列表 + 新建/编辑/删除操作
  - `src/components/types/EdgeTypeModal.vue`：新建/编辑边类型 Modal 表单（含语义/有向/唯一目标/允许类型等属性）
  - `src/components/types/EdgeTypeFieldEditor.vue`：可视化字段管理（同 NodeTypeFieldEditor）
  - `src/views/TypesView.vue`：Tab 布局整合节点/边类型管理
  - 验收：`pnpm build` 通过；API `/admin/api/node-types` 和 `/admin/api/edge-types` 返回预置数据

- **M2-08 画布从类型面板拖拽创建节点 完成**
  - `src/api/node.ts`：Node CRUD API 封装（create/delete/list 等）
  - `src/api/case.ts`：新增 `snakeizeKeys` 函数（camelCase → snake_case 转换）
  - `src/api/http.ts`：新增请求拦截器，自动将请求数据从 camelCase 转为 snake_case
  - `src/components/canvas/TypePalette.vue`：节点类型面板，按分类（physical/virtual/cloud/application）展示可拖拽类型
  - `src/views/CanvasView.vue`：集成 TypePalette，拖拽到画布区域创建节点（调用 POST /nodes）
  - 验收：`pnpm build` 通过；直接 curl POST /admin/api/nodes 创建节点成功

- **M2-04 画布节点拖拽 → PATCH /nodes/{id}/position 完成**
  - `src/components/canvas/TopologyCanvas.vue`：监听 AntV X6 `node:moved` 事件，拖拽结束时触发
  - `src/views/CanvasView.vue`：新增 `handleNodeMoved` 处理函数，500ms 防抖后调用 `PATCH /nodes/{id}/position`
  - `onBeforeUnmount` 清理防抖定时器
  - 验收：`pnpm build` 通过；curl PATCH /admin/api/nodes/{id}/position 更新节点坐标成功

- **M2-05 画布自动保存 60s 完成**
  - `src/composables/useCanvas.ts`：新增 `saveError` 和 `lastSavedAt` 状态追踪
  - `src/components/canvas/CanvasToolbar.vue`：状态标签支持"已保存 HH:MM"时间戳和"保存失败（重试中）"红色提示
  - `src/views/CanvasView.vue`：新增 60s 自动保存定时器 `autoSaveTimer`，用户活动（拖拽/创建/缩放等）重置定时器
  - `markDirty()` 在所有修改操作后调用，自动保存触发时 `performAutoSave()` 执行全量位置保存
  - 验收：`pnpm build` 通过

- **M2-06 WS topology.saved 推送 完成**
  - `app/core/ws_hub.py`：新增 `WsHub` 类管理 WebSocket 连接和订阅，`broadcast_topology_saved()` 广播接口
  - `app/main.py`：挂载 WS router，添加 CORS 中间件
  - `app/admin/topology.py`：`save_canvas_positions` 改为 async，保存成功后调用 `broadcast_topology_saved(id)`
  - `src/ws/client.ts`：已有 `WsClient` 单例支持 `topology.saved` 主题订阅
  - `src/views/CanvasView.vue`：mount 时订阅 `topology.saved` 事件，可响应其他客户端的保存通知
  - 验收：后端启动无报错，前端 `pnpm build` 通过

- **M2-08a 节点属性编辑 完成**
  - `src/components/canvas/NodeAttrsPanel.vue`：右侧浮动属性面板，点击节点时显示，支持编辑 text/number/select/boolean 类型字段
  - `src/components/canvas/NodeAttrsModal.vue`：创建节点时弹窗，填写节点类型的必填字段
  - `src/components/canvas/TopologyCanvas.vue`：新增 `node:click` 事件，向父组件发送节点点击信号
  - `src/views/CanvasView.vue`：
    - 新增 `handleNodeClick` 处理节点选中逻辑
    - 创建节点时检查节点类型是否有必填字段，有则弹出 `NodeAttrsModal` 填写后再创建
    - 选中节点后右侧显示属性面板
  - 验收：`pnpm build` 通过

- **M2-08b 画布连线功能 完成**
  - `backend/app/admin/edge.py`：新增 `PUT /edges/{edge_id}/attrs` 接口，参考节点属性设置实现
  - `frontend/src/api/edge.ts`：新建，TypeScript 类型和 API 方法封装（含 setAttrs）
  - `frontend/src/components/canvas/EdgeAttrsPanel.vue`：右侧浮动边属性面板，点击边时显示
  - `frontend/src/components/canvas/TopologyCanvas.vue`：新增 `edge:click` 事件监听
  - `frontend/src/components/canvas/CanvasToolbar.vue`：新增连线按钮，支持激活/取消连线模式
  - `frontend/src/views/CanvasView.vue`：
    - 新增连线模式状态（connectingMode、connectingSourceId、connectingStep）
    - 点击工具栏连线按钮进入连线模式
    - 连线模式下点击节点选择源节点，再选择目标节点，自动使用第一个边类型创建边
    - 点击边显示属性面板
  - 验收：前端 `pnpm build` 通过，后端 import 正常

## 2026-04-18

### 已完成
- **M2-08c 节点图标与展示设计 完成**
  - 图标下方展示节点名称：已由 `frontend/src/utils/nodeShape.ts` 自定义 X6 节点形状实现（`markup` 含 icon + label，`label` 通过 `refY = ICON_SIZE + 4` 定位到图标下方，`textAnchor=middle`），创建/加载节点时 `label: node.name`，重新进入画布后名称与创建时一致
  - 图标按 `node_types.code` 映射：`frontend/src/utils/nodeIcons.ts` 提供 11 个行业常用 SVG（switch/router/server/firewall/port/hypervisor/vm/ecs/pod/container/app），未知 code 回退到通用占位 `SVG_DEFAULT`
  - **新增：鼠标悬停节点展示属性 Tooltip**（`frontend/src/components/canvas/TopologyCanvas.vue`）
    - 监听 X6 `node:mouseenter` / `node:mousemove` / `node:mouseleave` 事件
    - 250ms 悬停延迟防止划过节点时闪烁；节点点击/blank 按下/离开画布时立即隐藏
    - 悬停时按需调用 `nodeApi.get(id)` 拉取最新 attrs，根据 `node_types.fields` 映射字段 label 与 sortOrder 排序展示
    - Tooltip 内容：节点名称、类型、状态（online/offline/warning 着色）、DN、所有非空属性字段
    - 深色半透明背景 + `pointer-events: none`，不影响鼠标交互
    - 画布容器升级为 `.topology-canvas-wrapper`，`position: relative` 以便 Tooltip 绝对定位
  - 验收：`pnpm build` 通过（CanvasView chunk 573 KB 正常）

## 2026-04-19

### 已完成
- **M2-08d 画布节点/连线单独删除 完成**
  - `frontend/src/components/canvas/NodeAttrsPanel.vue`：panel-footer 新增"删除"按钮（`danger` 样式），emit `delete` 事件携带 `nodeId` + `nodeName`；footer 改为 `space-between` 排列
  - `frontend/src/components/canvas/EdgeAttrsPanel.vue`：panel-footer 新增"删除"按钮，emit `delete` 事件携带 `edgeId`
  - `frontend/src/views/CanvasView.vue`：
    - 新增 `confirmDeleteNode` / `confirmDeleteEdge`：弹出 `Modal.confirm` 二次确认；节点确认显示节点名称，边确认显示 `源节点名 → 目标节点名`
    - 新增 `performDeleteNode`：调 `nodeApi.delete`，成功后用 `graph.getConnectedEdges(cell)` 取出所有相连边先移除 Cell，再移除节点 Cell；若当前选中即为该节点则关闭属性面板；调 `markDirty()` + `resetAutoSaveTimer()`
    - 新增 `performDeleteEdge`：调 `edgeApi.delete`，成功后 `graph.removeCell(edge)`；若当前选中即为该边则关闭属性面板
    - 失败时 `message.error` 显示错误，画布状态不变
    - 新增 `handleKeydown` 全局 keydown 监听：Delete / Backspace 键触发当前选中节点或边的删除确认；焦点在 input/textarea/contenteditable 或连线模式激活时不触发
    - `onMounted` 注册监听，`onBeforeUnmount` 移除
    - 模板为两个 AttrsPanel 绑定 `@delete` 到对应的 confirm 函数
  - 验收：`pnpm build` 通过（CanvasView chunk 575 KB 正常）；后端无需改动（`DELETE /nodes/{id}`、`DELETE /edges/{id}` 已就绪，edges FK 有 `ON DELETE CASCADE` 级联）

- **M2-09 接口配置 CRUD API 完成**
  - `backend/app/admin/schemas/api_config.py`：Pydantic v2 模型（ApiConfigCreate/Update/Detail/Item/EnabledPatch/TopologyPatch + Response 包装）；method 限 `GET/POST/PUT/PATCH/DELETE`（自动大写），data_source 限 `sql/static`
  - `backend/app/admin/api_config.py`：7 个路由（Python 3.9 兼容）
    - `GET /admin/api/apis` — 分页列表，支持 `groupName/enabled/topologyId/method/path`（path 模糊）过滤
    - `GET /admin/api/apis/{id}` — 详情（含 sqlText + config JSON 解析）
    - `POST /admin/api/apis` — 新建（`api_{uuid12}` ID；**method+path 冲突返回 409 + code:40301**；`data_source=sql` 时 sqlText 必填；topology_id 存在性校验）
    - `PUT /admin/api/apis/{id}` — 更新（**修改后的 method+path 冲突返回 409**；无字段返回 400 + code:40001；不存在返回 404 + code:40301）
    - `PATCH /admin/api/apis/{id}/enabled` — 切换启用开关
    - `PATCH /admin/api/apis/{id}/topology` — 切换绑定拓扑（null 清空；不存在返回 404 + code:40101）
    - `DELETE /admin/api/apis/{id}` — 删除
  - 统一错误码：40301(不存在/method+path 冲突)/40001(无更新字段/sqlText 空)/40101(拓扑不存在)
  - `backend/app/main.py`：注册 `api_config_router`；`schemas/__init__.py` 导出 ApiConfig 系列
  - 已在 uvicorn 下完整验证：创建/查询/过滤/PATCH enabled/PATCH topology（含无效拓扑 40101）/PUT（含改 method 冲突 40301）/DELETE/重复删除（40301）全部符合预期
  - Postman 验收脚本：`test_scripts/postman_api_configs.json`（30 个测试用例，覆盖 SETUP/CRUD/冲突/过滤/PATCH/异常/CLEANUP 全流程，可在 Postman Collection Runner 内顺序跑）

- **M2-10 接口列表页 + 启用开关 完成**
  - `frontend/src/composables/useApiConfigs.ts`：新增 composable，状态 `items/total/loading/page/pageSize/method/enabled/path/topologyId`；暴露 `fetchApis/toggleEnabled/deleteApi/onPageChange/onFilterChange`；`toggleEnabled` 调 `apiConfigApi.patchEnabled(id, value)` 并自动刷新列表
  - `frontend/src/components/apis/ApiConfigTable.vue`：Ant Design Vue Table，列含名称/方法/路径/分组/数据源/启用/更新时间/操作
    - 方法列用 `Tag` 按 HTTP 动词着色：GET=green、POST=blue、PUT=orange、PATCH=purple、DELETE=red
    - 启用列为 `Switch` 切换即触发 `patchEnabled` 并回显成功/失败提示（立即生效）
    - 数据源列：sql→蓝色 Tag "SQL"、static→"静态"
    - 路径用 `<code>` 等宽样式展示，分组为空显示 `-`
    - 工具栏：路径模糊搜索 Input、方法下拉、状态下拉（启用/禁用）、搜索 + 重置按钮、右侧刷新按钮；Select 允许 clear（使用字符串 value `'true'/'false'` 规避 ant-design-vue Select 不接受 boolean 的类型限制，提交前映射回 boolean）
    - 操作列：带 Popconfirm 的"删除"红字链接
    - 分页统一 `showSizeChanger` + `showQuickJumper` + `showTotal`
  - `frontend/src/views/ApisView.vue`：替换占位 Empty，挂载 `ApiConfigTable` + 接线 `useApiConfigs`；`handleToggleEnabled` 回显"已启用/已禁用"，`handleDelete` 回显"删除成功"；http interceptor 统一处理错误 toast
  - 验收：`pnpm build` 通过（ApisView chunk 6.01 KB，整体体积变化可忽略）；方法着色与 Switch 即时切换均符合 M2-10 验收标准
  - 未在本任务实现：新建/编辑弹窗（属于 M2-11 scope）

- **M2-10 新增接口弹窗 补充**（调整：M2-10 验收标准扩展新增能力，编辑留到 M2-11）
  - `docs/开发方案.md`：M2-10 验收标准扩展，明确"新建弹窗"字段集、数据源切换隐显 sqlText、topologyId 下拉、冲突处理
  - `frontend/src/composables/useApiConfigs.ts`：新增 `createApi(data)` 调 `apiConfigApi.create` 并刷新列表
  - `frontend/src/components/apis/ApiConfigModal.vue`：新建弹窗
    - 字段：name / method(Select) / path / groupName / dataSource(static|sql) / topologyId(Select，按需加载 `GET /topologies?pageSize=200`) / sqlText(TextArea，仅 dataSource=sql 时显示) / enabled(Switch)
    - 校验：name/method/path 必填；path 以 `/` 开头（正则）；dataSource=sql 时 sqlText 必填
    - topologyId 支持 `show-search` + `option-filter-prop="label"`，提交时 `null` 表示未绑定
    - 提交成功后关闭；失败时 rethrow，保持弹窗打开（M2-09 后端 409/40301 冲突由 http 拦截器已 toast 提示）
  - `frontend/src/components/apis/ApiConfigTable.vue`：工具栏右侧新增 "新建" 主按钮（PlusOutlined），emit `create`
  - `frontend/src/views/ApisView.vue`：挂载 `ApiConfigModal` + `handleCreate/handleModalSubmit`；创建成功后 `message.success('创建成功')`，失败 rethrow
  - 验收：`pnpm build` 通过（ApisView chunk 10.31 KB，CSS 0.45 KB）；新建流程：填表 → 提交 → 表格顶部显示新记录；method+path 冲突时后端返回 40301 拦截器 toast，弹窗保持打开

- **M2-11 接口编辑弹窗 + SQL 编辑器 + 参数映射 完成**
  - `docs/开发方案.md`：M2-11 验收标准细化（编辑复用新建弹窗 / CodeMirror 6 / 参数映射字段集 / method 编辑不可改 / PUT 提交范围 / 冲突处理）
  - 依赖：`pnpm add vue-codemirror codemirror @codemirror/lang-sql @codemirror/view`
  - `frontend/src/components/apis/SqlEditor.vue`：CodeMirror 6 薄封装
    - 用 `vue-codemirror` + `sql()` 扩展提供 SQL 语法高亮
    - `EditorView.lineWrapping` 开启软换行；`indent-with-tab` + `tab-size=2`
    - 外层 div 边框 + focus-within 高亮（模拟 antd Input 视觉），内部 `.cm-scroller` 覆写为等宽字体
    - Props：modelValue / placeholder / minHeight(默认 140px) / disabled；v-model 兼容
  - `frontend/src/components/apis/ParamMappingTable.vue`：参数映射表
    - 列：参数名 / 位置(query|path|body Select) / 类型(string|int|bool Select) / 必填(Switch) / SQL 绑定名 / 操作(删除)
    - 右上"新增参数"按钮 append 一行空白；行内编辑直接 emit `update:modelValue`
    - Select/Switch 的 `@update:value/@update:checked` 用 `unknown` 参数接收再 cast，规避 ant-design-vue `SelectValue/CheckedType` 包含 `undefined|string` 的类型不兼容
    - Row key 用 index（当前不支持拖拽排序，暂时足够）
  - `frontend/src/components/apis/ApiConfigModal.vue`：重构为新建 + 编辑双模式
    - 新增 `apiId` 可选 prop，非空即进入编辑模式
    - 打开时 `apiConfigApi.get(id)` 拉详情回填，`Spin` loading 期间禁止交互；`config.params` 解析为 ParamMapping[]
    - 编辑模式 method 下拉 + topologyId 下拉均 `disabled`（method 禁改避免冲突，topologyId 改动走列表 PATCH 入口，此处加 hint 文字说明）
    - SQL 模式下 TextArea 替换为 `<SqlEditor>`，并新增 `<ParamMappingTable>` 绑定 `formState.params`
    - 事件由单一 `submit` 拆成 `createSubmit` / `updateSubmit`；编辑时只传可变字段（name/path/groupName/dataSource/sqlText/config）；`buildConfig()` 仅在 SQL 模式且有 params 时序列化
    - 失败 rethrow 保持弹窗打开，由 http 拦截器 toast
  - `frontend/src/composables/useApiConfigs.ts`：新增 `updateApi(id, data)` 调 `apiConfigApi.update` 并刷新
  - `frontend/src/components/apis/ApiConfigTable.vue`：操作列新增"编辑"链接，emit `edit(id)`；列宽 100→140
  - `frontend/src/views/ApisView.vue`：`editingApiId` 状态；`handleEdit(id)` 打开弹窗；`handleCreateSubmit` / `handleUpdateSubmit` 分开回显"创建成功"/"更新成功"
  - 验收：`pnpm build` 通过，ApisView chunk 440.38 KB（CodeMirror 语法包 + Lezer parser 打包体积符合预期，gzip 143.87 KB）；编辑流程：列表点击"编辑" → 弹窗 Spin 加载 → 表单回填 → 修改 → 提交 PUT → toast"更新成功"
  - 未在本任务实现：编辑模式下 method/topologyId 修改入口（method 不支持修改；topologyId 走 `PATCH /apis/{id}/topology`，留 M2-12 或列表内联操作）

- **M2-12 动态 CTE 视图生成 完成**
  - `backend/app/core/cte_builder.py`：纯函数式 CTE 构造模块
    - `is_valid_ident(s)`：SQL 标识符白名单（`^[A-Za-z_][A-Za-z0-9_]*$`），阻止注入
    - `build_node_type_cte(code, type_id, field_keys)`：固定列 `id/name/dn/status/created_at/updated_at` + 按 `node_type_fields.sort_order` 展开的属性列 `MAX(CASE WHEN a.field_key='k' THEN a.value END) AS k`；JOIN `node_attrs`；WHERE `topology_id = :__tid__ AND node_type_id = '<literal type_id>'`；GROUP BY `n.id`
    - `build_edge_type_cte(...)`：边固定列 + `source_name/source_dn/target_name/target_dn`（JOIN nodes 两次）+ 属性打平
    - `build_generic_ctes()`：永远输出 `nodes`（全量字段 WHERE topology_id）/ `edges`（同上）/ `children`（基于 `edge_types.semantic='contain'` 的 JOIN，包含 `parent_id` + 目标节点简要字段）
    - `collect_views(conn, topology_id)`：
      - `SELECT DISTINCT nt.id, nt.code, nt.name FROM node_types nt JOIN nodes n ON n.node_type_id=nt.id WHERE n.topology_id = ?`（仅返回**拓扑中出现的**类型；空拓扑则 node_views/edge_views 为空，generic 仍返回）
      - 对每个类型查 fields（按 sort_order + id 排序，过滤非法标识符）
      - 跳过 `code` 与通用名冲突的类型（`nodes/edges/children`）或标识符非法的类型
      - 边类型进一步跳过与已生成节点视图同名的情况
  - `backend/app/admin/schemas/sql_helper.py`：`SqlViewItem{name, columns, sql}` / `SqlViewsData` / `SqlViewsResponse`
  - `backend/app/admin/sql_helper.py`：`GET /admin/api/sql/views?topologyId=X`
    - 拓扑存在性校验：不存在 → 404 + `code:40101`
    - 返回结构：`{nodeViews[], edgeViews[], generic[]}`，每项含 name/columns/sql（SQL 片段为 SELECT body，caller 负责 `WITH <name> AS (...)` 包装）
  - `backend/app/main.py`：注册 `sql_helper_router`；`schemas/__init__.py` 导出 `SqlViewItem/SqlViewsData/SqlViewsResponse`
  - 手工验证（curl）：
    - 含数据的拓扑 → 6 个 nodeViews（app/container/ecs/hypervisor/pod/server）+ 1 个 edgeView（connects）+ 3 个 generic（nodes/edges/children），所有 sql 引用 `:__tid__`
    - 拓扑不存在 → 404 `{code:40101, message:"拓扑不存在"}`
    - 缺 topologyId → 422
  - Postman 验收脚本：`test_scripts/postman_sql_views.json`（11 个用例 SETUP/业务拓扑视图/空拓扑/404/422/CLEANUP；业务拓扑预置 switch + router 节点 + physical_link 边以覆盖 nodeView / edgeView 校验）

- **Pydantic schema camelCase 兼容（M2-12 后修复）**
  - 背景：Postman 跑 M2-12 测试脚本时，第三步"创建 switch 节点"返回 422——根因是后端 `NodeCreate` / `EdgeCreate` 等只接 snake_case，而 `docs/API接口设计.md` 契约是 camelCase。前端能跑是因为 `frontend/src/api/case.ts` 拦截器把 camel→snake 翻译了，掩盖了 schema 的契约漂移
  - 修复：
    - `backend/app/admin/schemas/_base.py`：新增 `CamelModel(BaseModel)`，`model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True)`
    - `topology.py / node.py / edge.py / node_type.py / api_config.py / sql_helper.py`：所有入参/出参 schema 改为继承 `CamelModel`，同时保留 snake_case 兼容（前端 case.ts 不动也能用）
    - `test_scripts/postman_sql_views.json`：边创建步骤 URL 由 `POST /edges` 修正为 `POST /topologies/{topologyId}/edges`（与文档对齐），body 去掉冗余 `topologyId`
  - 验证（curl）：
    - `POST /admin/api/nodes` 用 `{topologyId, nodeTypeId, name}` → 200 ✓
    - `POST /admin/api/topologies/{tid}/edges` 用 `{edgeTypeId, sourceId, targetId}` → 200 ✓
    - `GET /admin/api/sql/views` 链路完整：node_views=[router,switch] / edge_views=[physical_link] / generic=[nodes,edges,children]
  - **遗留已清（见下条）**：节点路由契约漂移已对齐文档

- **响应序列化统一为 camelCase**
  - 背景：上条 schema 修复后，不同路由的 model_dump 行为不一致（topology via response_model → camelCase；node/edge/api_config 手写 dump → snake_case）
  - 修复：
    - `app/admin/topology.py / node.py / edge.py / api_config.py`：所有 `model_dump(mode="json")` → `model_dump(mode="json", by_alias=True)`
    - `app/admin/node_type.py`：list_node_types / get_node_type / list_edge_types / get_edge_type 原本直接把 Pydantic model 塞进 data 字段（依赖 FastAPI 默认 by_alias=True），显式改为 `item.model_dump(mode="json", by_alias=True)` 与其他路由保持一致
    - `data.model_dump(exclude_unset=True)` 用于 UPDATE SQL 字段抽取的保持 snake_case 不变（DB 列名本身是 snake_case）
  - 验证（curl，全链路 camelCase）：
    - POST /topologies → `createdAt / updatedAt / stats.nodeCount / stats.edgeCount` ✓
    - GET /node-types → `renderMode / dnTemplate / fieldKey / fieldLabel / fieldType / defaultValue / sortOrder` ✓
    - GET /nodes/{id} → `topologyId / nodeTypeId / createdAt / updatedAt` ✓
    - GET /edges/{id} → `edgeTypeId / sourceId / targetId` ✓
    - GET /topologies/{id}/graph → `canvasNodes` + 节点/边内全部 camelCase ✓
    - GET /nodes?topologyId=X 列表分页 → items[] 内部全 camelCase，外层 `pageSize` 保持 camelCase ✓
  - 前端影响：`frontend/src/api/case.ts` 的 snake→camel 转换器对无下划线的 key 是 no-op，不用动

- **节点路由对齐嵌套契约（与 edges 一致）**
  - 背景：`docs/API接口设计.md` 写的是嵌套 `POST /admin/api/topologies/{topologyId}/nodes`，原实现是平铺 `/nodes` 且 topologyId 在 body——契约漂移。边路由早已用嵌套，节点路由对齐
  - 风格选择：list/create 嵌套（topology 集合下的子资源），detail/update/delete/position/attrs 保持扁平（id 唯一，前缀冗余），与 edges 同构
  - 修改：
    - `app/admin/schemas/node.py`：`NodeCreate` 删除 `topology_id` 字段（由路径提供）
    - `app/admin/node.py`：
      - `GET /nodes` → `GET /topologies/{topology_id}/nodes`（参数 `topology_id` 从 Query 改为 Path；新增拓扑存在性校验 → 404/40101；`node_type_id` / `status` / 分页保持为 Query）
      - `POST /nodes` → `POST /topologies/{topology_id}/nodes`（topology_id 取自路径；body 不再接受 topology_id）
      - 其余 `GET/PUT/DELETE /nodes/{id}` + `/position` + `/attrs` 均保持扁平，未变
    - `frontend/src/api/node.ts`：`list` / `create` 改走嵌套 URL；`create(data)` 签名改为 `create(topologyId, data)`；`NodeCreate` 接口删除 `topologyId` 字段
    - `frontend/src/components/canvas/NodeAttrsModal.vue`：唯一调用方改为 `nodeApi.create(props.topologyId, {...})`
    - `test_scripts/postman_sql_views.json`：SETUP 创建节点两步走嵌套 URL
    - `test_scripts/postman_edges.json`：SETUP 创建 3 个节点走嵌套 URL + body 改 camelCase
    - `test_scripts/postman_nodes.json`：整体重写——所有 create/list 走嵌套；response 断言从 `topology_id / node_type_id` 改为 `topologyId / nodeTypeId`；新增"GET 拓扑不存在 → 404/40101"用例；原"按 topology_id 过滤"用例删除（topology 已在路径内）
  - 验证（curl，嵌套链路）：
    - `POST /topologies/{tid}/nodes` body `{nodeTypeId, name, dn}` → 200，`data.topologyId` 从路径派生 ✓
    - `POST /topologies/topo_xxx/nodes` → 404 / 40402 拓扑不存在 ✓
    - `GET /topologies/{tid}/nodes` → camelCase items ✓
    - `GET /topologies/topo_xxx/nodes` → 404 / 40101 ✓
    - `GET /nodes/{id}` 扁平详情 → 200（未动）✓
    - `POST /nodes` 旧平铺路由 → 404（已完全移除）✓
    - 前端 `pnpm build` 通过

- **M2-13 SQL 执行器 + 白名单 完成**
  - `backend/app/core/sql_executor.py`：纯函数模块
    - `validate_readonly(sql)`：去 `--` / `/* */` 注释、trim、去尾 `;` 后，若仍含 `;` 判多语句拒绝；首 6 字符大写须 `SELECT` 或 `WITH`，否则抛 `SqlValidationError`
    - `assemble_sql / _build_paged_sqls`：始终把用户 SQL 作为子查询包裹（SQLite 允许子查询内部 WITH，顶层 CTE 仍可见），避免拆解用户 CTE 列表；保留参数绑定的字符串 SQL
    - `execute_paged(conn, topology_id, user_sql, params, page, pageSize)`：
      - 调 `validate_readonly` → 400/40303
      - 用 `collect_views()` 拉齐 CTE prefix；写两条 SQL：`<WITH> SELECT COUNT(*) FROM (<user>) _t` 和 `<WITH> SELECT * FROM (<user>) _t LIMIT :__limit__ OFFSET :__offset__`
      - 绑定参数字典：过滤掉保留名 `__tid__ / __limit__ / __offset__`（用户不能覆盖），再注入这 3 个系统变量
      - 返回 `{items, total, page, pageSize, generatedSql, boundParams, durationMs}`
    - `preview(...)`：同上但不执行，返回 `{generatedSql, countSql, boundParams}` 供前端调试面板
  - `backend/app/admin/sql_helper.py`：新增
    - `POST /admin/api/sql/preview` / `POST /admin/api/sql/execute`
    - 公共 helper `_check_topology` 复用于 views / preview / execute，拓扑不存在统一 404/40101
    - `sqlite3.Error` 捕获映射为 400/40302 "SQL 执行失败: {原始错误}"
  - `backend/app/admin/schemas/sql_helper.py`：`SqlPreviewRequest / SqlExecuteRequest(CamelModel)`；`params: dict[str, Any]` 默认空字典；`pageSize` 1~1000 限制
  - **连带修复：generic CTE 循环引用**
    - 现象：`nodes AS (SELECT ... FROM nodes WHERE topology_id = :__tid__)` 被 SQLite 判成自引用报 `circular reference: nodes`
    - 根因：WITH 子句定义的 CTE 会 shadow 同名基表；`FROM nodes` 在 CTE 内部解析到 CTE 自身
    - 修复：`backend/app/core/cte_builder.py::build_generic_ctes` 中 `nodes/edges/children` 的 body 全部改用 schema 限定 `main.nodes / main.edges / main.edge_types` 绕过 shadowing；node_type / edge_type CTE 不受影响（名字不冲突）
  - **Postman 验收**：`test_scripts/postman_sql_executor.json` 21 请求 / 28 断言全绿
    - execute 成功路径：generic nodes（total=2）、node-type `switch`（1 条）、参数绑定 `:status=online`、分页 pageSize=1 第 1/2 页、用户自带 WITH（count=2）
    - 白名单拒绝：INSERT / UPDATE / DELETE / DROP / PRAGMA → 400/40303；多语句 `SELECT 1; SELECT 2` → 400/40303 "不允许多语句"
    - SQL 语法错 `FRO` → 400/40302
    - 拓扑不存在 → 404/40101
    - preview：只返回 generatedSql/countSql/boundParams；非只读同样 40303
  - **M2-12 回归**：`test_scripts/postman_sql_views.json` 重跑 11 请求 / 18 断言全绿（cte_builder `main.*` 限定未影响视图元数据输出）
  - 验收对齐：`docs/开发方案.md §2.3 / §2.4` 列出的白名单规则与分页行为全部满足；`docs/API接口设计.md` 错误码 40302 / 40303 语义对齐
  - 未实现：前端 SQL 编辑器 UI + SQL 调用面板（在 M3 数据查看页里接入），本任务仅完成后端执行器 + 契约验证

- **M2-14 接口测试执行 `POST /apis/{id}/test` 完成**
  - `backend/app/core/response_template.py`：新增响应模板渲染器（纯函数）
    - 占位符：`{{items}} / {{total}} / {{page}} / {{pageNo}}（page 别名） / {{pageSize}} / {{uuid}} / {{now}}`
    - 整串匹配（如 `"data": "{{items}}"`）→ 原值注入（保持数组/对象类型）；子串匹配 → 文本替换，scalar 用 `str`，dict/list 用 `json.dumps(ensure_ascii=False)`
    - 支持入参为已解析好的 dict/list 或原始 JSON 字符串；字符串非法 JSON 抛 `TemplateRenderError`（→ 40303）
    - 未知占位符原样保留，避免真实 JSON 内容里的 `{{...}}` 误触发
  - `backend/app/admin/schemas/api_config.py`：新增 `ApiTestRequest{headers,query,body}` / `ApiTestResult{statusCode,headers,body,durationMs,generatedSql,boundParams}` / `ApiTestResponse`，继承 `CamelModel` 与其他 schema 对齐
  - `backend/app/admin/api_config.py`：`POST /admin/api/apis/{id}/test`
    - `data_source=sql`：
      1. 校验 `topology_id` 已绑定 + `sql_text` 非空（否则 400/40001）
      2. 按 `config.params`（同时兼容 M2-11 表格的 `{name,in,type,required,bindTo}` 与 API 文档的 `{param,sqlParam,default}` 两种形态）从 `query/header/body` 拉值，`default` 兜底；按 `type` 做 `int/bool/string` 类型强转
      3. 按 `config.pagination.{pageNoParam,pageSizeParam,defaultPageSize}` 从 query 取分页（兜底 page=1 / pageSize=50，并裁剪到 ≤1000）
      4. 复用 `sql_executor.execute_paged`；`SqlValidationError → 40303`、`sqlite3.Error → 40302`
      5. 用 `config.response.template` 渲染（缺模板时兜底 `{code,data:{{items}},total:{{total}},pageNo:{{pageNo}},pageSize:{{pageSize}}}`）；非法模板 → 40303
      6. 响应含 `statusCode / headers(Content-Type) / body / durationMs / generatedSql / boundParams`
    - `data_source=static`：优先 `config.staticBody`，缺失时渲染 `config.response.template`（items=[]，total=0），再无则 `{code:0,data:null}`
    - **不写 request_logs**（连 `connect()` 只读即可，无 transaction）
    - 异常码：40301(接口不存在) / 40302(SQL 执行失败) / 40303(非 SELECT / 模板非法 JSON) / 40001(未绑定拓扑或 sqlText 为空)
  - **Postman 验收**：`test_scripts/postman_api_test.json` 19 请求 / 40 断言全绿
    - SQL 模式：无过滤 total=2、`status=online` 过滤 total=1、`pageNo=2 pageSize=1` 分页第二页；`{{uuid}}` 32 位 hex 校验、`{{now}}` ISO 格式校验；`body.data` 保持数组类型（模板整串替换语义生效）；`generatedSql / boundParams.__tid__` 存在
    - static 模式：`body.data.msg="hello"`，`generatedSql=null`，`boundParams={}`
    - 异常：INSERT → 400/40303；SELECT 语法错 → 400/40302（message 含"SQL 执行失败"）；`api_missing_xxx` → 404/40301
  - **M2-13 回归**：`test_scripts/postman_sql_executor.json` 重跑 21 请求 / 28 断言全绿（执行器 + CTE prefix 未受影响）
  - 对齐 `docs/API接口设计.md §2.6` 测试执行请求/响应契约；`docs/开发方案.md §2.5` 响应模板渲染 6 占位符全部生效
  - 未实现：前端接口配置页的"测试"按钮入口（属于 M2-11 扩展或 M3 工作量；后端契约已就绪，前端可直接调用）

## 2026-04-20

### 已完成
- **M2-11b 接口 SQL 编辑器视图面板（M2-12 接入 UI）**
  - 背景：M2-11 的 SQL 编辑器虽然有语法高亮，但用户在新建/编辑时看不到拓扑里有哪些 CTE 视图与列名，无法正确构造 SQL 与响应模板映射。M2-12 后端 `GET /admin/api/sql/views` 已就位，本任务把它接到 UI
  - `frontend/src/api/sql.ts`：新增
    - `SqlViewItem{name, columns[], sql}` / `SqlViewsData{nodeViews, edgeViews, generic}`
    - `fetchSqlViews(topologyId)`：直接拼 URL `?topologyId=xxx`，**故意绕开 http.ts 的全局 `snakeizeKeys` 对 params 的转换**（否则会把 `topologyId` 转成 `topology_id`，而后端 FastAPI `Query(alias="topologyId")` 只认驼峰 → 422）
  - `frontend/src/components/apis/SqlEditor.vue`：通过 `@ready` 事件保留 CodeMirror `EditorView` 实例引用；`defineExpose({insertAtCursor, focus})`
    - `insertAtCursor(text)` 用 `view.dispatch({changes:{from,to,insert}, selection:{anchor:from+len}})` 在当前光标位置插入文本，并自动聚焦编辑器；无 view 时回退到追加到 modelValue 末尾
  - `frontend/src/components/apis/SqlViewPanel.vue`：可折叠视图面板
    - Collapse 三段：节点视图 / 边视图 / 通用视图；默认全部展开；加载态、空态、错误态分别走 `Spin` + `Empty`
    - 每个视图头显示 `<name>` 可点击（→ 插入视图名），右侧有 "SELECT *" 按钮 → 插入 `SELECT * FROM <name>`
    - 列名以 `Tag` 展示（等宽字体），点击插入列名
    - 右上刷新按钮 + 首次加载与 `topologyId` 变化时自动拉取
    - 未绑定拓扑时显示"请先绑定拓扑以加载视图"，避免误操作
  - `frontend/src/components/apis/ApiConfigModal.vue`：
    - SQL 模式下 modal 宽度由 720px 变 1100px（computed `modalWidth`）
    - "SQL 语句" 表单项内部改为左右两栏：左 `SqlViewPanel`（260px 定宽，max-height 420px），右 `SqlEditor`（自适应 + minHeight 200px）
    - 通过 `ref="sqlEditorRef"` + `sqlEditorRef.value?.insertAtCursor(text)` 把面板 `@insert` 事件桥接到编辑器光标
    - 静态模式 modal 仍然 720px 不变，无视图面板干扰
  - 验收：`pnpm build` 通过（ApisView chunk 445 KB，新增 CSS ≈ 3.3 KB）；后端 `curl /admin/api/sql/views?topologyId=X` 返回预期视图元数据（手工验证）
  - **未实现**：视图面板与参数映射表的联动（点击列名回填到 `{{items}}[0].<col>` 模板这一步属于步骤 2 的"运行预览按钮"范畴）；编辑时视图面板对 `topology_id` 的变更没有重新拉取——当前表单拓扑在编辑时被 `disabled`，不会变化，无需处理

- **M2-11c 接口 SQL 编辑器运行预览按钮（M2-13 接入 UI）**
  - 背景：M2-11b 接上了视图面板让用户能看到表结构；本任务让用户能在未保存接口之前就执行 SQL 查看结果，直接辅助构造响应模板映射
  - `frontend/src/api/sql.ts`：扩展
    - `SqlExecuteRequest{topologyId, sql, params?, page?, pageSize?}` / `SqlExecuteResult{items, total, page, pageSize, generatedSql, boundParams, durationMs}`
    - `executeSql(body)` 走 `apiPost('/sql/execute', …)`，POST body 由 `CamelModel` 接两种拼写，**无需**绕开 snakeizeKeys（与 M2-11b `fetchSqlViews` 的 URL query 场景不同）
    - 默认 `page=1 / pageSize=10`，避免前端预览一次拉太多
  - `frontend/src/components/apis/SqlRunner.vue`：运行预览卡片（SFC）
    - Props：`topologyId` / `sqlText` / `params?: ParamMapping[]`；emit `insert(text)` 给父组件转发到编辑器
    - `canRun = topologyId && sqlText.trim()`；disabled 时按钮带 Tooltip "请先绑定拓扑并填写 SQL"
    - `paramsToDict()` 把 `ParamMapping[]` 展开为 `{bindTo 或 name: null}`——预览阶段默认值为 null，让 `:p IS NULL OR col = :p` 这类可选过滤语句能跑通
    - Toolbar："运行预览" primary 小按钮 + 元数据文本"共 N 条 · 展示前 M · X ms"
    - 错误态：捕获 `ApiError` 走 `[code] message` 格式，`Alert type="error"` 就地展示（不干扰 http 拦截器的全局 toast）
    - 空结果态："查询成功，但无数据返回"
    - 成功态：
      - 结果列 Tag 行（等宽字体，点击插入列名到编辑器光标）
      - `Table` 横向滚动 + 200px 竖向 + 小号字体 + 等宽字体 + ellipsis + `customRender` 把 null/undefined 显示 `—`、object 走 `JSON.stringify`
      - `Collapse` "查看生成的 SQL" 面板：深色代码块展示 `generatedSql`，下方显示 `boundParams` 便于排查参数绑定
  - `frontend/src/components/apis/ApiConfigModal.vue`：把 `SqlRunner` 挂到右栏 SqlEditor 下方，把 `@insert` 桥接到已有的 `handleInsertFromPanel`；表单内无需新增字段（参数映射已由 `ParamMappingTable` 提供）
  - 验收：
    - `pnpm build` 通过（ApisView chunk 449 KB，+4 KB）
    - 后端 `curl -X POST /admin/api/sql/execute -d '{"topologyId":"…","sql":"SELECT * FROM nodes"}'` 返回 items / total / columns / generatedSql / boundParams 全字段
    - 手工回归：绑定拓扑 → 输入 SQL → 点击"运行预览"命中 `/admin/api/sql/execute` → 表格展示结果 → 点列名 Tag 光标位置插入列名；空 SQL / 未绑定拓扑 按钮 disabled
  - **未实现**：结果列到响应模板的自动映射（点击列 Tag 插入到响应模板编辑器中 `{{items}}[0].<col>` 这一步）——当前模板字段还在 `config.response.template` JSON 里由人工书写，UI 没有专门编辑器；该映射属于 M3 或后续 UX 增强

- **M2-11d 响应模板编辑 TextArea（接入 M2-14，方案 1）**
  - 背景：M2-11c 后用户能预览 SQL 结果，但仍无法在 UI 里定义响应体格式——只能通过 REST 客户端手写 `config.response.template` JSON。本任务落"最小可用"方案：加一个 TextArea 占位
  - `frontend/src/components/apis/ApiConfigModal.vue`：
    - `FormState` 扩展 `responseTemplate: string`；`emptyForm()` 初始化为空串
    - `loadDetail()` 从 `detail.config.response.template` 回填；若历史数据误存为对象/数组则 `JSON.stringify(…, null, 2)` 格式化展示（向后兼容）
    - `buildConfig()` 仅在 SQL 模式且 textarea trim 非空时写入 `config.response = {template: <trimmed>}`；否则完全不写，由 M2-14 后端默认模板兜底
    - `templateParseError` computed：`JSON.parse(trim())` 实时校验，失败时下方 hint 变红展示报错；**不阻塞提交**（用户可能在草稿状态，最终由后端 40303 兜底）
    - 新增 Form.Item 在"参数映射"之后，仅 SQL 模式显示：`Input.TextArea rows=8`、等宽字体、`spellcheck="false"`、占位符展示默认兜底模板
    - 占位符提示行用 `v-pre` 规避 Vue 对 `{{...}}` 的模板插值，直接以字面量展示 6 个占位符 + 整串 vs 子串两种语义说明
    - 新增 CSS：`.hint-error`（红 #d4380d）、`.tpl-textarea :deep(textarea)`（Consolas 等宽、12px、line-height 1.5）
  - 静态模式留白：M2-14 后端已有 `config.staticBody` 优先逻辑，但本任务不动静态模式的响应体编辑器——另起 M2-11e 或并入 M4 收尾
  - 验收：
    - `pnpm build` 通过（ApisView chunk 450 KB，+1 KB）
    - 手工回归：新建接口不填模板 → `GET /apis/{id}` 返回 `config={}`；回到编辑弹窗模板区为空，占位符展示默认；填入合法 JSON 模板 → 保存 → 再打开编辑弹窗内容回填一致；故意填 `{"a":}` → 下方红色 hint 显示解析错误，仍可提交（后端 40303 拦截）
    - 与 M2-14 `POST /apis/{id}/test` 串联：保存带自定义模板的接口后调 test 接口，响应体按模板渲染
  - **未实现**：
    - 静态模式 `config.staticBody` 编辑器（另列 M2-11e 或 M4）
    - 结果列 → 响应模板字段的可视化映射（需要模板 AST + 拖拽，属方案 3，另列 M2-11f 或并入 M4 UX 增强）
    - 模板内 JSON 语法高亮（当前是纯 TextArea，后续可换 CodeMirror JSON 模式）

### 下一步
- M3-01 `/mock/**` 动态路由注册（复用 api_configs + sql_executor + response_template；启用/禁用开关实时生效；method+path 冲突拦截）
- 或 M2-11e 静态模式 `staticBody` 编辑器（方案 1 风格，TextArea + JSON.parse 校验）
