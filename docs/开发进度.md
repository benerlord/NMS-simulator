# 开发进度

## 阶段 7 — HTTP 接口配置前端 (已完成 2026-04-13)

### 7.1 接口配置列表页
- 文件: `frontend/src/pages/ApiConfig/index.tsx`
- 表格展示接口配置（方法色彩标签 / 路径 / 名称 / 分组 / 数据源类型 / 启用开关）
- 分组 Tab 切换筛选
- 关键词搜索 / 新建 / 导入 / 导出 / 删除 / 启用切换

### 7.2 接口配置编辑表单
- 文件: `frontend/src/pages/ApiConfig/ConfigForm.tsx`
- Tabs 分四块: 基本信息 / 数据源 / 响应模板 / 故障模拟
- SQL 模式: 快速加载默认 SQL（devices/ports/links/alarms/metrics）、Monaco SQL 编辑器、参数绑定、分页配置
- Static 模式: Monaco JSON 编辑器
- 响应模板: Monaco JSON 编辑器（支持 `{{items}}` `{{total}}` `{{pageNo}}` `{{pageSize}}` 占位符）
- 故障模拟: 延迟 Slider、错误率 Slider、错误状态码
- 文件: `frontend/src/pages/ApiConfig/MonacoField.tsx` — Monaco Editor 包装

### 7.3 测试执行面板
- 文件: `frontend/src/pages/ApiConfig/TestPanel.tsx`
- 动态参数输入（根据接口配置的参数定义预填）
- 调用 `/api/api-configs/{id}/test` 执行
- 展示: 执行 SQL / 查询结果表格 / 渲染结果 JSON / 执行耗时

## 阶段 8 — 数据/日志/设置页面 (已完成 2026-04-13)

### 8.1 数据查看页
- 文件: `frontend/src/pages/DataViewer/index.tsx`
- Tabs: 设备 / 告警 / 指标
- 拓扑选择下拉 + 关键词搜索
- 对应后端: `/api/topologies/{id}/devices`, `/alarms`, `/metrics`

### 8.2 日志查看页
- 文件: `frontend/src/pages/LogViewer/index.tsx`
- 请求日志表格（方法色彩标签、状态码色彩、耗时、错误列）
- 方法/路径过滤，刷新、导出、清空
- 详情 Drawer 展示完整请求头/请求体/响应体（格式化 JSON）

### 8.3 系统设置页 (集成在 Settings 页 Tab 1)
- 后端新增 API: `backend/app/api/settings.py` (GET/PUT `/api/settings`)
- 前端按 category 分组展示: general / auto_save / history / log
- 布尔值自动渲染为 Switch, 其他渲染为 Input
- 批量保存

### 8.4 协议管理 (集成在 Settings 页 Tab 2)
- 协议卡片: 状态标签、启动/停止/重启按钮
- HTTP Mock 配置展示 (host/port)
- 活跃路由列表

### 8.5 Token 管理 (集成在 Settings 页 Tab 3)
- 认证配置: Token 有效期、凭据列表（增删改）
- 活跃 Token 列表（创建/过期时间、删除、清空）
- 测试登录区（验证凭据匹配）

### 仪表盘
- 文件: `frontend/src/pages/Dashboard/index.tsx`
- 统计卡片: 拓扑数 / 接口配置数 / 请求日志数 / 协议状态
- 协议服务列表 + 全部启动/停止按钮

## 新增前端 API 模块
- `frontend/src/api/apiConfigs.ts` — 包含 api-configs / sessions / protocols / settings 全部调用

## 构建验证
- TypeScript 编译 + Vite 构建: 通过（1,838 KB / gzip 567 KB）
- Playwright 浏览器测试: 全部页面加载正常、Drawer 可打开、Settings 三个 Tab 可切换

## 修复
- `vite.config.ts` 代理规则从 `/api` 改为 `^/api/.*`，否则 `/api-config` 前端路由会被代理到后端造成 404

## 测试汇总
- 后端单元测试: **44 个全部通过**（test_core / test_api / test_phase5 / test_phase6）
- 前端构建 + 运行时验证: **通过**

## 阶段 9 — WebSocket 实时推送 (已完成 2026-04-12)

### 9.1 后端 WebSocketManager
- 文件: `backend/app/services/websocket_manager.py`
- 频道映射: topology / alarm / metric / request_log / protocol / system / config
- 订阅 EventBus 通配符 `*`，按事件名前缀路由到频道
- subscribe / unsubscribe / ping 消息协议
- 死连接自动清理

### 9.2 WebSocket 端点
- 文件: `backend/app/api/websocket.py`
- `GET /ws?channels=xxx,yyy`
- 断连安全回收

### 9.3 前端 useWebSocket Hook
- 文件: `frontend/src/hooks/useWebSocket.ts`
- 指数退避重连（1s → 30s 封顶）
- 30s 心跳 ping
- 返回 `{ status, send }`

### 9.4 连接状态指示器
- 文件: `frontend/src/components/WsStatusIndicator.tsx`
- 绿/黄/红 三态 Badge，置于 AppLayout Header

### 9.5 实时数据页面
- Dashboard: 监听 protocol / request_log / topology，事件驱动 refetch
- LogViewer: 监听 request_log，第一页时自动刷新

## 阶段 10 — 端到端集成与部署 (已完成 2026-04-13)

### 10.1 E2E 测试
- 文件: `backend/tests/test_phase10_e2e.py`
- 5 个场景：完整业务流（拓扑→配置→Mock 启动→外部真实 HTTP 请求→日志落库）、鉴权失败、SQL 错误、故障注入、版本回滚

### 10.2 静态托管
- `backend/app/main.py`：检测到 `frontend/dist/` 时，挂载 `/assets` 并为非 `/api`、非 `/ws` 路径提供 SPA fallback

### 10.3 部署脚本与文档
- `scripts/start.sh` / `scripts/start.bat`：构建前端 + 启动后端的一体化脚本
- `scripts/dev.sh`：开发双进程（后端 reload + vite dev）
- `README.md`：技术栈、目录结构、快速开始、测试说明
- `.gitignore`：Python / Node / 数据 / IDE 忽略项

### 修复
- `backend/app/dal/memory_store.py::full_sync / sync_table`：新增 `db.in_transaction` 守卫，避免嵌套事务触发 `cannot start a transaction within a transaction`
- `test_topology_version_restore`：按 `device_count == 1` 选择还原版本（之前误用 `min(version)` 选到了创建拓扑时的空快照）

## 测试汇总（最终）
- 后端测试: **53 个全部通过**（test_core / test_api / test_phase5 / test_phase6 / test_phase9 / test_phase10_e2e）
- 包括 WebSocket 订阅/广播端到端测试与 HTTP Mock 真实请求 E2E 测试
